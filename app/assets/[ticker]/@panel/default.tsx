/** D2: sin ninguna ruta interceptada activa, el slot @panel no renderiza nada — el drawer solo existe cuando hay un detalle abierto (ver DetailPanel). La columna derecha normal vive siempre en page.tsx, no acá. */
export default function PanelDefault() {
  return null
}

/** D2: sin ninguna ruta interceptada activa, el slot @panel no renderiza nada — el drawer solo existe cuando hay un detalle de tesis abierto (ver DetailPanel). El listado de tesis normal vive siempre en page.tsx, no acá. */
export default function PanelDefault() {
  return null
}
